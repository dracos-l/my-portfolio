# Shared sticky-note board (server-side only — do not prefix these with NEXT_PUBLIC_)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Content moderation
OPENAI_API_KEY=

# Email notification: use a verified Resend sender for NOTES_EMAIL_FROM.
RESEND_API_KEY=
NOTES_EMAIL_TO=
NOTES_EMAIL_FROM=

# Owner-only delete controls (use a unique, long password)
NOTES_ADMIN_PASSWORD=
